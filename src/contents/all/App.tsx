import { hot } from 'react-hot-loader/root';
import React from 'react';

// 同步数据
// i18n.sync();

const App = () => {
    return (
        <div className="app">
            Hello
        </div>
    );
};

export default hot(App);
