import React from 'react';
import { Alert } from 'antd';

import './index.scss';

const SuperDiscount = () => {
    return (
        <div className="superdiscount_content">
            <Alert message="Ken" description="hello, world!" type="info" />
        </div>
    );
};

export default SuperDiscount;
